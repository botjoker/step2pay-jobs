# Scheduler service instructions

Перед работой в этом каталоге полностью прочитай корневой `../CLAUDE.md`, затем
локальный `CLAUDE.md`. Локальный файл — источник истины по polling-loop,
уведомлениям, базе данных и ограничениям проверки Go-сервиса.

Не запускай `go build` и `go run`.
